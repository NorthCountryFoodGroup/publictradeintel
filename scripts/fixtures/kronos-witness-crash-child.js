"use strict";
const guard=require("./kronos-s3-network-guard").denyExternalNetwork(),t=require("./kronos-witness-evidence"),data=JSON.parse(process.argv[2]);
let x;try{if(data.stage==="witness-owner-only"){x=t.openWitness(require("node:path").join(data.root,"witness.sqlite"),{...t.config(),mode:"open-existing",vault:{latest:()=>null}});x.close();process.exit(0);}x=t.open(data.root,{mode:"open-existing",hook:stage=>{if(stage===data.stage&&stage!=="acknowledgment-returned")process.exit(77);}});if(data.stage==="ownership-only"){x.close();process.exit(0);}x.witness.append(t.request(t.eventHistory()));if(data.stage==="acknowledgment-returned")process.exit(77);x.close();guard.assertClean();}catch(e){console.log(/^[A-Z_]+$/.test(e.code||"")?e.code:"FIXTURE_FAILURE");process.exit(2);}
